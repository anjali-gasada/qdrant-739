How each proposal item is covered

Proposal itemImplementation3-node + 5-node clustersdeploy/docker-compose.3node.yml and deploy/docker-compose.5node.yml — both with cap_add: NET_ADMIN so the chaos harness can run iptables.Replication & write-consistency sweepscripts/run_full_sweep.sh enumerates (RF, WCF) pairs and re-creates the collection for each.Fault injection (kill + partition)cmd/chaos/main.go + internal/chaos/chaos.go. SIGKILL via docker kill -s SIGKILL; partition via iptables -A INPUT -p tcp --dport 6335 -j DROP inside the target container, then -F to heal. The RecoveryWatcher polls /cluster every 100ms and emits a leader-timeline JSON.max_segment_size_kb impactscripts/run_compaction_sweep.sh tries {10000, 50000, 200000, default} — recreates the collection, reloads, waits for status=green, runs Workload B at c=25 for 60s.Pipeline bottleneck localizationqclient.Client.Wait flag distinguishes WAL-acked (fast path) from index-acked (full path) writes. The runbook gives the exact two-run sequence to subtract them.Batch-size sweepmake bench-batch and the explicit loop in the runbook section 8.Concurrency sweep {1,10,15,25,30}cmd/bench/main.go's default sweep grid.6 workloads (A-F)internal/workload/workload.go — per-profile op CDFs, all three operation kinds (search/update/insert/RMW/scan-with-filter).5 APIs (Add, Search, TopK, Delete, AddMany)internal/qclient/client.go — Add(), AddMany(), Search(), Delete(). TopK is Search with the Limit parameter.SIFT-128-Euclidean dataset, train+test+neighborsinternal/dataset/sift.go reads all three HDF5 datasets. Distance is Distance_Euclid, not Cosine — important detail.Tail latency p95/p99HDR histograms with bounded precision (1µs - 60s, 3 sig-fig). The reservoir-sampling approach in some Python frameworks would distort the tail; HDR doesn't.